include: configs/base.yaml

data:
  task: forecasting
  lookback: 336
  horizon: 96
  stride: 1

optim:
  epochs: 20
  batch_size: 4
  accumulation_steps: 4
  early_stopping_patience: 5

metrics:
  primary: mse
  report: [mse, mae, rmse]
  scale: normalized
